// script.js

// Get the "Choose a Car" button by its ID
const chooseCarButton = document.getElementById("choose-car-button");

// Add a click event listener to the button
chooseCarButton.addEventListener("click", function () {
    alert("You clicked the 'Choose a Car' button!");
});

// Get the "Login" button by its ID
const loginButton = document.getElementById("login-button");

// Add a click event listener to the button
loginButton.addEventListener("click", function () {
    alert("You clicked the 'Login' button!");
});

// Get the "Sign Up" button by its ID
const signUpButton = document.getElementById("signup-button");

// Add a click event listener to the button
signUpButton.addEventListener("click", function () {
    alert("You clicked the 'Sign Up' button!");
});
